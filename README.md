# ballistic-ide
A minimal IDE built with electron.

### Download
Copy this into your terminal where you want the project built:
```bash
git clone https://github.com/marcushcarter/ide.git
cd ide
npm install
npm start
npm run build
```
The output will be in `dist/win-unpacked/`

